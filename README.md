# Klado
Web app for home storage with QR-labeled boxes, global search, and no app install required
Smart home inventory system built with ASP.NET Core + React.
Scan a QR code on any box → instantly see what's inside, no app install needed.
Hierarchical storage (home → room → shelf → box) with full-text and semantic search.
